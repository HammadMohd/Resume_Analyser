# This file tells nixpacks what to do with the app
# Auto-detected: Python project

# Install dependencies
RUN pip install --no-cache-dir .

# Start the app
CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8080"]
